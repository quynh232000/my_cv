<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Nếu là file hoặc thư mục thật thì không rewrite
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d

  # Redirect mọi request còn lại về index.html
  RewriteRule ^ index.html [L]
</IfModule>